# Email sent to Scholars'' Bank users when they BTE batch import fails.
#
# Parameters: {0} the export error
#             {1} the URL to the feedback page
#
#
# See org.dspace.core.Email for information on the format of this file.
#
#set($subject = "${config.get('dspace.name')} - The batch import was not completed")
The batch import you initiated from the ${config.get('dspace.name')} UI was not completed, due to the following reason:
 ${params[0]}

For more information you may contact your system administrator:
 ${params[1]}

The ${config.get('dspace.name')} Team
