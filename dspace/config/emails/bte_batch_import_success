## Email sent to Scholars'' Bank users when they successfully batch import items via BTE.
##
## Parameters: {0} the filepath to the mapfile created by the batch import
##
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')} - Batch import successfully completed")
The batch item import you initiated from the Scholars'' Bank UI has completed successfully.

You may find the mapfile for the import in the following path: ${params[0]}

${config.get('dspace.name')}