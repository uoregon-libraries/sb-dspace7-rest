## E-mail sent to Scholars'' Bank users when new items appear in collections they are
## subscribed to
##
## Parameters: {0} is the details of the new collections and items
## See org.dspace.core.Email for information on the format of this file.
#set($subject = "${config.get('dspace.name')}: Subscription")
New items are available in the collections you have subscribed to:

${params[0]}

${config.get('dspace.name')}